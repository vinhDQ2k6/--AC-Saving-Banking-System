import { expect } from "chai";
import { describe } from "mocha";

describe("SavingPlan (placeholder)", function () {
  it("placeholder test", async function () {
    expect(true).to.equal(true);
  });
});
